# Én nøglefil pr. titel: kopiér til .env.<slug> (fx .env.pulsen, .env.spaending)
# med en separat OpenRouter-nøgle pr. magasin, så forbruget kan aflæses pr. titel
# i OpenRouter-dashboardet. Chefredaktør-agenten bruger titlens egen nøgle.
OPENROUTER_API_KEY=
